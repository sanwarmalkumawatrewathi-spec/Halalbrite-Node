<div class="card">
    <div class="card-header">
        <h3>User Management</h3>
        <a href="/admin/users/add" class="btn btn-primary btn-sm"><i class="fa-solid fa-plus"></i> Add New User</a>
    </div>
    <div class="table-responsive">
        <table>
            <thead>
                <tr>
                    <th>User Info</th>
                    <th>Role</th>
                    <th>Preferences</th>
                    <th>Status</th>
                    <th>Joined</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% if (users.length === 0) { %>
                    <tr><td colspan="6" class="text-center text-muted" style="padding: 3rem;">No users found.</td></tr>
                <% } else { %>
                    <% users.forEach(user => { %>
                        <tr>
                            <td>
                                <div class="customer-info">
                                    <span class="customer-name"><%= user.username %></span><br>
                                    <span class="customer-email"><%= user.email %></span>
                                </div>
                            </td>
                            <td>
                                <span class="badge" style="background: #eff6ff; color: #1e40af;"><%= user.roles && user.roles.length > 0 ? user.roles.map(r => r.name).join(', ') : 'No Role' %></span>
                            </td>
                            <td>
                                <div style="display: flex; gap: 5px;">
                                    <span class="badge" style="background: <%= user.preferences?.eventUpdates ? '#dcfce7' : '#f3f4f6' %>; color: <%= user.preferences?.eventUpdates ? '#166534' : '#9ca3af' %>;" title="Event Updates">
                                        <i class="fa-solid fa-calendar-check"></i>
                                    </span>
                                    <span class="badge" style="background: <%= user.preferences?.promotions ? '#dcfce7' : '#f3f4f6' %>; color: <%= user.preferences?.promotions ? '#166534' : '#9ca3af' %>;" title="Promotions">
                                        <i class="fa-solid fa-tags"></i>
                                    </span>
                                    <span class="badge" style="background: <%= user.preferences?.newsletter ? '#dcfce7' : '#f3f4f6' %>; color: <%= user.preferences?.newsletter ? '#166534' : '#9ca3af' %>;" title="Newsletter">
                                        <i class="fa-solid fa-envelope-open-text"></i>
                                    </span>
                                </div>
                            </td>
                            <td>
                                <span class="badge <%= user.status === 'active' ? 'badge-success' : 'badge-danger' %>">
                                    <%= user.status.charAt(0).toUpperCase() + user.status.slice(1) %>
                                </span>
                            </td>
                            <td class="text-muted"><%= new Date(user.createdAt).toLocaleDateString() %></td>
                            <td>
                                <div style="display: flex; gap: 8px;">
                                    <a href="/admin/users/edit/<%= user._id %>" class="btn btn-sm btn-ghost" title="Edit User"><i class="fa-solid fa-pen-to-square"></i></a>
                                    <form action="/admin/users/delete/<%= user._id %>" method="POST" onsubmit="return confirm('Delete this user?')">
                                        <button type="submit" class="btn btn-sm btn-ghost" style="color: var(--danger);" title="Delete"><i class="fa-solid fa-trash"></i></button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    <% }) %>
                <% } %>
            </tbody>
        </table>
    </div>
</div>
